#include <stdio.h>
int main()
{

    int x = 2;
    int y = 3;
    int z = 3;
    int k = 1;
    printf("%d\n", 3*x/y- z+k);
   // left to right associativity is used in this expression, so the expression is evaluated as follows:
    return 0;
}