#include <stdio.h>
 int main() {
char st[20];

gets(st) ; // return the string that we write :
printf("%s" , st);
puts(st); // it returns the string without print function:

printf("Are you okay ?"); // printf after puts is written in a new line : 
  return 0;
 
 }