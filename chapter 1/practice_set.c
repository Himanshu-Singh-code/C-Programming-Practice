// Program to calculate the area of a rectangle 
#include <stdio.h>

int main() {
    int breadth = 6;
    int length = 10;
    printf("area of rectangle is %d", breadth * length);
    return 0; }

// Output: area of rectangle is 60

 