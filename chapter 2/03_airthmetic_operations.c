#include <stdio.h>

 int main() {
   // we can do any airthmetic operations in c language 
  float a = 100.0 , b = 47.0 ;

  float c = a/b ; // except % modulus - it doesnt work with float or double data types
  printf("the value of a is %f \t value of b is %f  \t and sum is %f",a,b,c);

  return 0;
}